# Secrets & local config
.env

# Python
__pycache__/
*.py[cod]
.venv/
*.egg-info/
.mypy_cache/
.ruff_cache/
.pytest_cache/
.coverage
.coverage.*
htmlcov/

# Runtime data
/data/
sandbox/out/
sandbox/contracts/uploads/
sandbox/contracts/staged_patches/
sandbox/cache/
sandbox/broadcast/
sandbox/deployments/
sandbox/.anvil.pid
sandbox/.anvil.log

# Transient run artifacts (regenerated by `make demo` / `make bench` / `make verify`)
demo/scenarios/last_run.json
demo/scenarios/benchmark.json
demo/scenarios/patch_verification.json

# Local debug/inspection artifacts (Playwright MCP, screenshots)
.playwright-mcp/
warroom-fixed.png

# Devpost submission pack — kept local, not published in the repo
docs/SUBMISSION.md

# Internal design + build-process docs — kept local, not published
architecture.md
CLAUDE.md
