# Foundry project config for the SENTINEL simulation sandbox (architecture.md §5.2).
# SimulationMCP deploys demo contracts here against a local Anvil fork.
[profile.default]
src = "contracts"
out = "out"
libs = ["lib"]
optimizer = true
optimizer_runs = 200
